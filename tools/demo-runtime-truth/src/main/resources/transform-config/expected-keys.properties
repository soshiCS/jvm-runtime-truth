# Target XOR key for each operation type (informational only, not used by TransformCompiler).
# Actual keys are sourced from transform-keys.properties via hash-based selection.
# If observed output is garbled or fails the printable-ASCII check, the active transform
# likely has the wrong key value.
target.default.key=127
