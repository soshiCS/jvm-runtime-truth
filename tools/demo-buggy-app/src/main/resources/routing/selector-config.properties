# Polynomial hash seed for processor routing.
# The seed initialises the rolling hash in ProcessorSelector.select().
# Changing this value will reroute ALL requests — update only during planned maintenance.
# See ProcessorSelector.java for the hash function definition.
routing.hash.seed=44
